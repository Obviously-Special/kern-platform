# Issue Tracker

Known issues that need fixing. One entry per issue, kept updated with what
we've verified so the next session can pick up without archaeology.

**No open issues at this time.** The previous entry (BUG-001, review-page
price breakdown not visible in a live browser) was resolved: the fix
shipped and was verified in a clean browser, and the regression is
covered by the `review-price-total` / `review-surcharge-explanation` eval
scenarios.

## How to report a bug

1. What you did (page, question/action, in order)
2. What you expected
3. What happened instead
4. Browser + console: press F12 → Console → paste anything red, plus the
   `[kern] sdk …` line (tells us which SDK version the page is running)
